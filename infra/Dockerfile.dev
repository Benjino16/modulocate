# Shared dev image for backend/worker/portal/vote — all part of the same
# pnpm workspace, so all need the full workspace installed for `workspace:*`
# deps to resolve. No CMD: each compose service supplies its own `command:`.
FROM node:22-alpine

RUN corepack enable

WORKDIR /app

COPY . .

RUN pnpm install --frozen-lockfile
